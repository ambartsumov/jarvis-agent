# ═══════════════════════════════════════════════════════════════════════════════
# PDS-Ultimate v2 — Dependencies (minimal + optional heavy)
# ═══════════════════════════════════════════════════════════════════════════════

# Core
python-dotenv>=1.0.0
SQLAlchemy>=2.0.0
APScheduler>=3.10.0
httpx>=0.27.0
aiofiles>=24.1.0
python-dateutil>=2.9.0

# Telegram
aiogram>=3.10.0
aiohttp-socks>=0.9.0
Telethon>=1.36.0

# Google Calendar / Gmail API (two-way sync)
google-api-python-client>=2.130.0
google-auth>=2.30.0
google-auth-oauthlib>=1.2.0

# Browser (optional)
playwright>=1.45.0

# MCP (Model Context Protocol) — external tool servers, OpenClaw bridge
# mcp>=1.0.0

# Advanced browser automation (OpenManus-level, optional)
# browser-use>=0.1.0  — install with: pip install browser-use && playwright install chromium

# Voice — lightweight (Vosk small ~45MB, lazy load)
vosk>=0.3.45

# Documents (optional modules)
pandas>=2.2.0
openpyxl>=3.1.0
python-docx>=1.1.0
PyPDF2>=3.0.0

# Hybrid OpenManus bridge (WebSocket IPC with OpenClaw layer)
websockets>=13.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Heavy optional — install manually if needed:
# faster-whisper>=1.0.0
# easyocr>=1.7.0
